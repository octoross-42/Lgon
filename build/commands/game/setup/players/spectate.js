import { CONSTANTES } from "../../../../config/constantes.js";
export function run(bot, message, argv) {
    // console.log(message.author);
    // console.log(message.guild);
}
export const help = CONSTANTES.COMMANDS.GAME.SETUP.PLAYERS.SPECTATE;
